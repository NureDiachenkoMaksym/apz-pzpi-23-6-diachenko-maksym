events {}
http {
  upstream nra_api {
    server api:8000;
  }

  server {
    listen 80;
    location / {
      proxy_pass http://nra_api;
      proxy_set_header Host $host;
      proxy_set_header X-Real-IP $remote_addr;
    }
  }
}
